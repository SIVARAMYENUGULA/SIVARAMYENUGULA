# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/placemux

# JWT - STABLE FOR DEV (do NOT change these once users have logged in)
JWT_SECRET=placemux-dev-jwt-secret-do-not-use-in-production-abc123xyz
JWT_REFRESH_SECRET=placemux-dev-jwt-refresh-secret-do-not-use-in-production-def456uvw
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS - allow local dev servers
CORS_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:3000
FRONTEND_URL=http://localhost:5173

# SMTP (optional - for email verification/password reset)
SMTP_USER=shabirsk989@gmail.com
SMTP_PASS=qztb ooqi pdbq phve
SMTP_FROM=PlaceMux <shabirsk989@gmail.com>

# Backend public URL (for file uploads)
BACKEND_PUBLIC_URL=http://localhost:5000
